import { FunctionComponent } from 'react';
import { BaseProps } from '../type';
interface RingsProps extends BaseProps {
    radius?: string | number;
}
export declare const Rings: FunctionComponent<RingsProps>;
export default Rings;
