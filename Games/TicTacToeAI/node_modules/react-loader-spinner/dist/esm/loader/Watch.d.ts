import { FunctionComponent } from 'react';
import { BaseProps } from '../type';
interface WatchProps extends BaseProps {
    radius?: string | number;
}
declare const Watch: FunctionComponent<WatchProps>;
export default Watch;
