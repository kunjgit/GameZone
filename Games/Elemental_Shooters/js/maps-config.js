Game.mapsConfig = [];

Game.mapsConfig['arcade'] = [];

Game.mapsConfig['arcade'][0] = {
  enemyWeapons: ['pistol'],
  rowsMin: 20,
  rowsMax: 25,
  colsMin: 20,
  colsMax: 25,
  mapType: 'air',
  dropProbability: [true, false],
  dropWeapons: ['machineGun'],
  enemyHealth: 4
};

Game.mapsConfig['arcade'][1] = {
  enemyWeapons: ['pistol'],
  rowsMin: 22,
  rowsMax: 23,
  colsMin: 22,
  colsMax: 23,
  mapType: 'air',
  dropProbability: [true, false],
  dropWeapons: ['machineGun', 'shotGun'],
  enemyHealth: 4
};

Game.mapsConfig['arcade'][2] = {
  enemyWeapons: ['shotGun', 'machineGun'],
  rowsMin: 25,
  rowsMax: 28,
  colsMin: 25,
  colsMax: 28,
  mapType: 'air',
  dropProbability: [true, false, false],
  dropWeapons: ['machineGun', 'shotGun', 'revolver'],
  enemyHealth: 6
};

Game.mapsConfig['arcade'][3] = {
  enemyWeapons: ['shotGun', 'machineGun'],
  rowsMin: 28,
  rowsMax: 30,
  colsMin: 28,
  colsMax: 30,
  mapType: 'air',
  dropProbability: [true, false, false],
  dropWeapons: ['machineGun', 'shotGun', 'revolver', 'rockedLauncher'],
  enemyHealth: 8
};

Game.mapsConfig['arcade'][4] = {
  enemyWeapons: ['shotGun', 'machineGun'],
  rowsMin: 30,
  rowsMax: 32,
  colsMin: 30,
  colsMax: 32,
  mapType: 'water',
  dropProbability: [true, false, false],
  dropWeapons: ['machineGun', 'shotGun', 'revolver', 'rockedLauncher'],
  enemyHealth: 10
};

Game.mapsConfig['arcade'][5] = {
  enemyWeapons: ['pistol', 'machineGun'],
  rowsMin: 25,
  rowsMax: 30,
  colsMin: 25,
  colsMax: 30,
  mapType: 'water',
  dropProbability: [true, false, false],
  dropWeapons: ['machineGun', 'shotGun', 'revolver', 'rockedLauncher'],
  enemyHealth: 10
};

Game.mapsConfig['arcade'][6] = {
  enemyWeapons: ['pistol', 'machineGun', 'revolver'],
  rowsMin: 28,
  rowsMax: 35,
  colsMin: 28,
  colsMax: 35,
  mapType: 'water',
  dropProbability: [true, false, false],
  dropWeapons: ['machineGun', 'shotGun', 'revolver', 'rockedLauncher'],
  enemyHealth: 12
};

Game.mapsConfig['arcade'][7] = {
  enemyWeapons: ['pistol', 'machineGun', 'revolver'],
  rowsMin: 30,
  rowsMax: 38,
  colsMin: 30,
  colsMax: 38,
  mapType: 'water',
  dropProbability: [true, false, false],
  dropWeapons: ['machineGun', 'shotGun', 'revolver', 'rockedLauncher'],
  enemyHealth: 14
};

Game.mapsConfig['arcade'][8] = {
  enemyWeapons: ['pistol', 'machineGun', 'revolver'],
  rowsMin: 32,
  rowsMax: 40,
  colsMin: 20,
  colsMax: 40,
  mapType: 'earth',
  dropProbability: [true, false, false],
  dropWeapons: ['machineGun', 'shotGun', 'revolver', 'rockedLauncher'],
  enemyHealth: 16
};

Game.mapsConfig['arcade'][9] = {
  enemyWeapons: ['pistol', 'machineGun', 'revolver', 'rockedLauncher'],
  rowsMin: 20,
  rowsMax: 40,
  colsMin: 30,
  colsMax: 35,
  mapType: 'earth',
  dropProbability: [true, false, false],
  dropWeapons: ['machineGun', 'shotGun', 'revolver', 'rockedLauncher'],
  enemyHealth: 16
};

Game.mapsConfig['arcade'][10] = {
  enemyWeapons: ['pistol', 'machineGun', 'revolver', 'rockedLauncher'],
  rowsMin: 20,
  rowsMax: 40,
  colsMin: 20,
  colsMax: 25,
  mapType: 'earth',
  dropProbability: [true, false, false],
  dropWeapons: ['machineGun', 'shotGun', 'revolver', 'rockedLauncher'],
  enemyHealth: 17
};

Game.mapsConfig['arcade'][11] = {
  enemyWeapons: ['pistol', 'machineGun', 'revolver', 'rockedLauncher'],
  rowsMin: 20,
  rowsMax: 40,
  colsMin: 20,
  colsMax: 30,
  mapType: 'earth',
  dropProbability: [true, false, false],
  dropWeapons: ['machineGun', 'shotGun', 'revolver', 'rockedLauncher'],
  enemyHealth: 18
};

Game.mapsConfig['arcade'][12] = {
  enemyWeapons: ['pistol', 'machineGun', 'revolver', 'rockedLauncher'],
  rowsMin: 20,
  rowsMax: 25,
  colsMin: 20,
  colsMax: 40,
  mapType: 'fire',
  dropProbability: [true, false, false],
  dropWeapons: ['machineGun', 'shotGun', 'revolver', 'rockedLauncher'],
  enemyHealth: 19
};

Game.mapsConfig['arcade'][13] = {
  enemyWeapons: ['pistol', 'machineGun', 'revolver', 'rockedLauncher'],
  rowsMin: 20,
  rowsMax: 25,
  colsMin: 35,
  colsMax: 40,
  mapType: 'fire',
  dropProbability: [true, false, false],
  dropWeapons: ['machineGun', 'shotGun', 'revolver', 'rockedLauncher'],
  enemyHealth: 20
};

Game.mapsConfig['arcade'][14] = {
  enemyWeapons: ['pistol', 'machineGun', 'revolver', 'rockedLauncher'],
  rowsMin: 20,
  rowsMax: 25,
  colsMin: 35,
  colsMax: 40,
  mapType: 'fire',
  dropProbability: [true, false, false],
  dropWeapons: ['machineGun', 'shotGun', 'revolver', 'rockedLauncher'],
  enemyHealth: 23
};

Game.mapsConfig['arcade'][15] = {
  enemyWeapons: ['pistol', 'machineGun', 'revolver', 'rockedLauncher'],
  rowsMin: 20,
  rowsMax: 25,
  colsMin: 35,
  colsMax: 40,
  mapType: 'fire',
  dropProbability: [true, false, false],
  dropWeapons: ['machineGun', 'shotGun', 'revolver', 'rockedLauncher'],
  enemyHealth: 25
};


Game.mapsConfig['survivor'] = [];

Game.mapsConfig['survivor'][0] = {
  enemyWeapons: ['pistol'],
  rowsMin: 20,
  rowsMax: 23,
  colsMin: 20,
  colsMax: 23,
  dropProbability: [true, false],
  dropWeapons: ['machineGun'],
  enemyHealth: 4
};

Game.mapsConfig['survivor'][1] = {
  enemyWeapons: ['pistol'],
  rowsMin: 22,
  rowsMax: 25,
  colsMin: 22,
  colsMax: 25,
  dropProbability: [true, false],
  dropWeapons: ['machineGun', 'shotGun'],
  enemyHealth: 6
};

Game.mapsConfig['survivor'][2] = {
  enemyWeapons: ['machineGun', 'shotGun'],
  rowsMin: 25,
  rowsMax: 32,
  colsMin: 25,
  colsMax: 32,
  dropProbability: [true, false, false],
  dropWeapons: ['machineGun', 'shotGun', 'revolver'],
  enemyHealth: 8
};

Game.mapsConfig['survivor'][3] = {
  enemyWeapons: ['machineGun', 'shotGun', 'revolver'],
  rowsMin: 25,
  rowsMax: 32,
  colsMin: 25,
  colsMax: 32,
  dropProbability: [true, false, false],
  dropWeapons: ['machineGun', 'shotGun', 'revolver', 'rockedLauncher'],
  enemyHealth: 10
};

Game.mapsConfig['survivor'][4] = {
  enemyWeapons: ['machineGun', 'shotGun', 'revolver', 'rockedLauncher'],
  rowsMin: 30,
  rowsMax: 35,
  colsMin: 30,
  colsMax: 34,
  dropProbability: [true, false, false],
  dropWeapons: ['machineGun', 'shotGun', 'revolver', 'rockedLauncher'],
  enemyHealth: 10
};

Game.mapsConfig['survivor'][5] = {
  enemyWeapons: ['machineGun', 'shotGun', 'revolver', 'rockedLauncher'],
  rowsMin: 40,
  rowsMax: 45,
  colsMin: 40,
  colsMax: 45,
  dropProbability: [true, false, false],
  dropWeapons: ['machineGun', 'shotGun', 'revolver', 'rockedLauncher'],
  enemyHealth: 10
};

Game.mapsConfig['survivor'][6] = {
  enemyWeapons: ['machineGun', 'shotGun', 'revolver', 'rockedLauncher'],
  rowsMin: 20,
  rowsMax: 50,
  colsMin: 20,
  colsMax: 50,
  dropProbability: [true, false, false],
  dropWeapons: ['machineGun', 'shotGun', 'revolver', 'rockedLauncher'],
  enemyHealth: 10
};